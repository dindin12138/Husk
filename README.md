# Husk

A lightweight, daemon-based Wayland terminal written in C++ and powered by libghostty.

## Building

Make sure you are in your development environment (e.g., `devenv shell`), then run the following commands to configure and build the project:

```bash
# Configure the build directory
cmake -B build -G Ninja

# Build the executable
cmake --build build
```
